# Ignore everything
*

# Allow all directories so included files inside them are not ignored
!*/

# Allow Python files anywhere
!**/*.py
!**/*.c
!**/*.h
!**/*.cpp
!**/*.hpp

# Allow README files (case-insensitive, with or without extension)
!README
!README.*
!readme
!readme.*

# Allow LICENSE files (case-insensitive)
!LICENSE
!LICENSE.*

# keep .gitignore itself
!.gitignore